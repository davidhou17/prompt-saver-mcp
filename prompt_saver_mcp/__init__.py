"""Prompt Saver MCP Server.

An MCP server that summarizes and saves conversation threads as prompts,
categorizes them by use case, and stores them in MongoDB with vector embeddings
for semantic search.
"""

__version__ = "0.1.0"
